{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "summary",
    "evidence_refs",
    "missing_information",
    "next_action"
  ],
  "properties": {
    "summary": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "enum": [
        "The check is not applicable because FireExit is explicitly false.",
        "The check cannot determine applicability because FireExit is unresolved.",
        "The model-declared opening-width proxy is at or above the configured screening threshold.",
        "The model-declared opening-width proxy is below the configured screening threshold.",
        "The finding concerns unavailable model-declared opening-width information.",
        "The finding concerns unusable model-declared opening-width information.",
        "The finding concerns an interrupted opening-width screening check.",
        "The metadata completeness check found a non-empty FireRating label and a readable SelfClosing boolean.",
        "The metadata completeness check found FireRating or SelfClosing missing or unreadable.",
        "The finding concerns an interrupted metadata completeness check.",
        "The response identifies the next manual source-model check for this finding."
      ]
    },
    "evidence_refs": {
      "type": "array",
      "minItems": 0,
      "maxItems": 20,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 300
      }
    },
    "missing_information": {
      "type": "array",
      "minItems": 0,
      "maxItems": 10,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1,
        "maxLength": 120,
        "enum": [
          "FireExit classification is missing.",
          "FireExit classification is invalid.",
          "FireExit classification is unresolved.",
          "OverallWidth is missing.",
          "Opening-width input or project length-unit information is unusable.",
          "FireRating metadata is missing.",
          "FireRating metadata is invalid.",
          "SelfClosing metadata is missing.",
          "SelfClosing metadata is invalid.",
          "Further diagnostic information for the opening-width check is unavailable.",
          "Further diagnostic information for the metadata check is unavailable."
        ]
      }
    },
    "next_action": {
      "type": "string",
      "minLength": 1,
      "maxLength": 140,
      "enum": [
        "Verify the FireExit property and source in the authoring model.",
        "Review the model-declared opening-width source in the authoring model.",
        "Verify the OverallWidth source and project length-unit definition in the authoring model.",
        "Repeat the deterministic opening-width check after reviewing the source model.",
        "Review the FireRating and SelfClosing sources in the authoring model.",
        "Verify the FireRating property and source in the authoring model.",
        "Verify the SelfClosing property and source in the authoring model.",
        "Verify the FireRating and SelfClosing properties and sources in the authoring model.",
        "Repeat the deterministic metadata check after reviewing the source model."
      ]
    }
  },
  "additionalProperties": false
}
